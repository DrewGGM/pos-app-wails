# PostgreSQL Database Configuration
# Copy this file to .env and update with your actual credentials

# Database Connection - Option 1: Use individual parameters
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=your_password_here
DB_NAME=posapp
DB_SSLMODE=disable

# Database Connection - Option 2: Use full connection URL (overrides individual parameters if set)
# For local PostgreSQL:
# DATABASE_URL=postgresql://postgres:password@localhost:5432/posapp?sslmode=disable

# For Neon (or other cloud PostgreSQL):
# DATABASE_URL=postgresql://username:password@host.neon.tech/database?sslmode=require

# For Supabase:
# DATABASE_URL=postgresql://postgres:password@db.xxx.supabase.co:5432/postgres

# For AWS RDS:
# DATABASE_URL=postgresql://username:password@xxx.rds.amazonaws.com:5432/database

# For Google Cloud SQL:
# DATABASE_URL=postgresql://username:password@/database?host=/cloudsql/project:region:instance

# WebSocket Server Configuration (for mobile/tablet integration)
WS_PORT=8080

# Rappi Webhook Server Configuration
# Port for receiving Rappi webhooks (orders, cancellations, etc.)
RAPPI_WEBHOOK_PORT=8081

# Config API Server Configuration
# Port for external configuration management REST API
CONFIG_API_PORT=8082
